
ats.senza server
================

The ats.senza server component does not use a configuration file, all the options are provided
through environment variables.

You can list the variables and their default values with the ``--write-config-defaults`` option:

.. program-output:: senza-server --write-config-defaults
   :prompt:

